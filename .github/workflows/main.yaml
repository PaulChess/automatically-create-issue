on:
  push:
  schedule:
    - cron: '41 9 * * *'

jobs:
  create_issue:
    runs-on: ubuntu-latest
    name: create issue automatically
    steps:
      - name: first step
        uses: PaulChess/automatically-create-issue@main
        with:
          TOKEN: ${{ secrets.TOKEN }}